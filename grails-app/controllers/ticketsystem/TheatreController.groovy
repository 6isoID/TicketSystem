package ticketsystem

import grails.converters.JSON

class TheatreController {

    def index() {
//        println "something happens"
       /* List<Session> sessions = Session.findAllByTimeGreaterThanEquals(new GregorianCalendar())
        def model = [:].withDefault {[]}// <= that's an Aggregator pattern XD. Do I really need an explicit class for THAT
        for(session in sessions) model[session.cinema].add session

        [cinemas : model] as JSON*/
    }

}
